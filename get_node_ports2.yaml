- shell: kubectl get svc --all-namespaces -o=jsonpath='{.items[*]}' | jq 'select(.spec.type=="NodePort") | {name:.metadata.name,ports:.spec.ports}' > /tmp/svc_ports.json
- shell: jq -s '{node_ports:[.[]]}' /tmp/svc_ports.json > /tmp/node_ports.json